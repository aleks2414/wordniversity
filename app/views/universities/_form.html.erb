<%= form_for(@university) do |f| %>
  <% if @university.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@university.errors.count, "error") %> prohibited this university from being saved:</h2>

      <ul>
      <% @university.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country %><br>
    <%= f.text_field :country_code, required: true %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.url_field :url, required: true %>
  </div>
  <div class="field">
    <%= f.label :university_name %><br>
    <%= f.text_field :name, required: true %>
  </div>
  <div class="actions">
    <%= f.submit class: "button button_red" %>
  </div>
<% end %>
